import 'package:flutter/material.dart';

class PointOfSaleView extends StatelessWidget {
  const PointOfSaleView({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(),
        body: const Placeholder(),
      ),
    );
  }
}
